import React, { Component } from 'react';

class Footer extends Component{

    render(){
        return(<div>
            my footer is in another component
        </div>);
    }
}
export default Footer;